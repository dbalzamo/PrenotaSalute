version: '3.8'

services:

  mysql:
    image: mysql:8.0
    container_name: MySQL-server
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: hospital_book_management
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    image: dariobalzamo02/prenotazione-medica:latest
    container_name: Medivue-service
    ports:
      - "8080:8080"
    # Variabili ${...}: Docker Compose le legge dal file .env nella stessa cartella del compose (o dall'ambiente shell).
    environment:
      DB_URL: jdbc:mysql://mysql:3306/hospital_book_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS}
    depends_on:
      mysql:
        condition: service_healthy

networks:
  default:
    name: prenotasalute-be_default

volumes:
  mysql_data: