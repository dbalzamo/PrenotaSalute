## Server Properties
server.port=8080
server.servlet.context-path=/prenotazione-medica

# ==== JWT ====
jwt.secret=======================BezKoder=Spring===========================
jwt.expirationMs=3600000
jwt.jwtCookieName=myCookie

# === Database ===
spring.application.name=PrenotazioneMedica
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_book_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=balzamo2002
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Hibernate Properties ===
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# === Logging ===
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF

# === Solo i tuoi log ===
logging.level.root=WARN
logging.level.prenotazione.medica=INFO